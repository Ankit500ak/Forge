'use client'

import { useRankTheme } from '@/lib/rank-theme-context'
import { useState } from 'react'

interface NextLevelProps {
  currentXP: number
  goalXP: number
  percentage: number
}

export function NextLevelSection({ currentXP, goalXP, percentage }: NextLevelProps) {
  // Component removed per request — render nothing to avoid duplicate UI
  return null
}
